package stobiecki.tamingtheasynchronousbeast.ex07_schedulers;

public class ParallelFlux {

    /**
     * TODO https://github.com/reactor/reactor-core#parallelflux
     * ParallelFlux can starve your CPU's from any sequence whose work can be subdivided in concurrent tasks.
     * Turn back into a Flux with ParallelFlux#sequential(), an unordered join or use arbitrary merge strategies via 'groups()'.
     */
}
